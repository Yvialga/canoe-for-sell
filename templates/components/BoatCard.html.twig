<a href="/boat/{{ id }}" class="boat-component">
 <div>
    <img src="{{ image }}" alt="Image illustrant l'annonce" width="" class="h-72 object-cover w-full rounded-t-md">
    <div class="px-4 py-2.5">
        <em class="not-italic">{{ price }}€</em>
        <h4 class="first-letter:uppercase">{{ title }}</h4>
        <div class="flex justify-between mt-2">
            <p>Particulier</p>
            <p>
                <span class="uppercase">{{ brand }}</span>
                - 
                <span>{{ type }}</span>
            </p>
        </div>
    </div>
 </div>
</a>